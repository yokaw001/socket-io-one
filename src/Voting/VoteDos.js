import React from 'react';



const VoteDos = (props) => {
   let assignedPrompts = props.assignedPrompts;
   let currPlayer = props.currPlayer;
    return (
        <div>

        </div>
    )
}

export default VoteDos;

