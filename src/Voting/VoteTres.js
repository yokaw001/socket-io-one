import React from 'react';



const VoteTres = (props) => {
   let assignedPrompts = props.assignedPrompts;
   let currPlayer = props.currPlayer;
    return (
        <div>

        </div>
    )
}

export default VoteTres;

